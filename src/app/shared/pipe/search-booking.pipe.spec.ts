import { SearchBookingPipe } from './search-booking.pipe';

describe('SearchBookingPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBookingPipe();
    expect(pipe).toBeTruthy();
  });
});
